{
  "name": "chat",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc && cp -r server/public dist/",
    "dev": "node dist/index.js --inspect",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "yarn run build && yarn run dev"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/mime": "^2.0.0",
    "mime": "^2.3.1",
    "mysql": "^2.16.0",
    "socket.io": "^2.1.1"
  },
  "devDependencies": {
    "@types/node": "^10.5.2"
  }
}
